name: Apply guarded product modules

on:
  push:
    branches: [main]
    paths:
      - '.content/products/**'
      - 'product_cards.py'
      - 'apply_product_cards.py'
      - 'validate_product_cards.py'
      - 'assets/product-cards.css'
      - 'assets/product-affiliate.js'
      - '.github/workflows/apply-product-modules.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply-and-validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Apply approved product modules
        run: python apply_product_cards.py all

      - name: Validate approved product-module scope
        run: python validate_product_cards.py all

      - name: Commit generated product modules
        run: |
          if git diff --quiet; then exit 0; fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add comparatifs usages guides
          git commit -m "content: apply guarded product modules"
          git pull --rebase origin main
          git push origin HEAD:main
